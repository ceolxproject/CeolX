# M8 Venue Subscriptions — QA Scenario Matrix

> Generated 18/08/2026 during implementation and the local end-to-end test run.
> Verified against **Stripe test mode** (`acct_1TYpwaPqyV6kc3X3`, sandbox), the **local
> Docker database** (`ceolx_dev`) and the **local backend** on `:3001`. Nothing in this
> run touched staging or production.
>
> `✅ Verified live` means it was actually executed and observed in that environment.
> `✅ Handled` means the code path exists and is unit-tested but was not exercised live.

---

## System Overview

A venue pays €19.99/month or €199/year after a **6-month free trial** with the card taken
up front. Nothing about payment appears in the app (Apple Rule 3.1.1): the venue taps
**Activate Profile**, the server emails a one-time link, and the link 302s straight to
Stripe Checkout. A Stripe webhook is the **only** writer of subscription state.

`venue_profiles.subscription_status` has five values, and visibility is derived from it —
never stored:

| Status      | Means                                | Publicly visible | May publish |
| ----------- | ------------------------------------ | ---------------- | ----------- |
| `inactive`  | Never completed payment setup        | No               | No          |
| `trialing`  | Card on file, inside the trial       | Yes              | Yes         |
| `active`    | Paying                               | Yes              | Yes         |
| `past_due`  | Charge failed, Stripe still retrying | Yes              | Yes         |
| `cancelled` | Stripe gave up, or period elapsed    | No               | No          |

**Key properties / configuration:**

| Property                            | Effect                                                                                                                                                          |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `VENUE_GATE_ENABLED`                | Master switch. `false` (shipping default) makes every venue visible regardless of status. Ships dark because every production venue is still `inactive` (O-08). |
| `STRIPE_TRIAL_DAYS` (183)           | Trial length, passed per Checkout Session — **not** on the Price, so D-42 can withhold it.                                                                      |
| `ACTIVATION_TOKEN_TTL_MINUTES` (45) | One-time link lifetime.                                                                                                                                         |
| `billing_blocked`                   | Boolean, not a status. Set on chargeback and **never cleared by a webhook**; overrides status everywhere.                                                       |
| Stripe retry schedule               | **This is the grace window** since D-33 was revised. Live-mode-only setting, currently **15 days**, D-33 wants **7**.                                           |
| `automatic_tax`                     | Deliberately **off** — €19.99 is VAT-inclusive but no VAT is calculated until Chongie registers (D-66).                                                         |

---

## GROUP A — Activation & Checkout

| #   | Scenario                                        | Expected Result                                                                                                                                                                                                                                                                                                            | Status           |
| --- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| A1  | Venue taps Activate Profile while `inactive`    | `venues.requestActivation` returns `{sentTo, expiresAt}` and **no URL** (D-16). Token stored as a SHA-256 hash; email carries two buttons with `plan=monthly` and `plan=annual`. Prices appear in the **email** only, which is the off-app channel D-16 permits                                                            | ✅ Verified live |
| A2  | Open the monthly activation link                | **302 to `checkout.stripe.com`**, zero bytes of our own HTML, `cache-control: no-store`, `referrer-policy: no-referrer`                                                                                                                                                                                                    | ✅ Verified live |
| A3  | Open the annual link from the **same** token    | 302 to Checkout with the annual price — one token serves both plans (D-63)                                                                                                                                                                                                                                                 | ✅ Verified live |
| A4  | Inspect the Checkout Session our server created | `mode=subscription`, `integration_identifier=ceolx-venue-subscription-krtmvhqz`, `customer_email` prefilled, dynamic payment methods (no `payment_method_types` sent), `automatic_tax.enabled=false`, `adaptive_pricing.enabled=false` (D-68), success/cancel URLs point at `/activate/complete` and `/activate/cancelled` | ✅ Verified live |
| A5  | Complete Checkout with `4242 4242 4242 4242`    | Subscription `trialing`; webhook writes `trialing`, `trial_ends_at` +183d, plan from the Price, and the Stripe customer id. **Verified on an Android device against the local backend, with the real Stripe payment sheet** — `Divy Venue Subscribe` → trialing / monthly / 2027-02-17 / cus_V5vhdBUZvw8rl7                | ✅ Verified live |
| A6  | Land on `/activate/complete` after paying       | HTML "Payment received", never the JSON envelope, and says nothing about the subscription being live. **Reached by a real card payment** — this is the page every paying venue previously saw as a raw JSON 404                                                                                                            | ✅ Verified live |
| A7  | Abandon Checkout → `/activate/cancelled`        | HTML page, states the card was not charged                                                                                                                                                                                                                                                                                 | ✅ Verified live |
| A8  | Venue selects annual at checkout                | `plan=annual` stored, invoice €199.00, renewal one year out                                                                                                                                                                                                                                                                | ✅ Verified live |

---

## GROUP B — Token Lifecycle & Re-attempts

| #   | Scenario                                               | Expected Result                                                                                                                                                                                      | Status                                        |
| --- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| B1  | Open a valid link, do not pay, reopen it               | Still works. Confirmed from the other side too: the token for a **completed** payment shows `consumed_at` stamped at payment time, not at page load (D-17 + D-24)                                    | ✅ Verified live                              |
| B2  | Request activation twice, then open the **older** link | 400 HTML "This link is no longer valid" — issuing a new token deletes prior unconsumed ones (D-18)                                                                                                   | ✅ Verified live (unknown-token path)         |
| B3  | Open a link after payment succeeded                    | 200 HTML "already been used" — success wording, not an error                                                                                                                                         | ✅ Handled                                    |
| B4  | Open a link older than 45 minutes                      | 410 HTML "This link has expired", offers a fresh one                                                                                                                                                 | ✅ Handled                                    |
| B5  | Malformed token (`?token=short`)                       | 400 HTML, **no DB query issued**                                                                                                                                                                     | ✅ Verified live                              |
| B6  | Missing `plan` parameter                               | 400 HTML — the token carries no plan, so it cannot be guessed (D-63)                                                                                                                                 | ✅ Verified live                              |
| B7  | `plan=weekly` or `plan=price_123`                      | 400 HTML — a Stripe Price ID is rejected as a plan                                                                                                                                                   | ✅ Verified live                              |
| B8  | Token containing `<script>`                            | 400, and the value is never reflected into the page                                                                                                                                                  | ✅ Verified live                              |
| B9  | Any `/activate` response                               | Token never appears in the body; stripped from the access log and from Sentry (`beforeSend` redacts `token`/`code`/`secret`)                                                                         | ✅ Verified live                              |
| B10 | Two activation requests in quick succession            | Second returns `TOO_MANY_REQUESTS`, not a failure — the client turns it into a cooldown. `/activate` itself sits on the dedicated `credentialRedeem` tier (10/15min), not the 60/min map-lookup tier | ✅ Verified live                              |
| B11 | Activation requested by a `billing_blocked` venue      | Refused — a disputed account is never invited back (D-51)                                                                                                                                            | ✅ Verified live (via publish/checkout guard) |

---

## GROUP C — Trial Lifecycle

| #   | Scenario                                              | Expected Result                                                                                                                                                                         | Status                                                               |
| --- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| C1  | Trial reaches its end with a good card                | `current_period_start` == `trial_end` **to the second**; status `trialing` → `active`; €19.99 charged and paid. **No window in which the venue is invisible** — both states are visible | ✅ Verified live                                                     |
| C2  | Trial ends on an annual plan                          | €199.00 charged, `current_period_end` one year later                                                                                                                                    | ✅ Verified live                                                     |
| C3  | Trial-ending warning, 7 days out                      | Daily sweep emails "Your CeolX trial ends soon — €199.00 on 23 August 2026" with the **plan's own amount**, interval and the real charge date                                           | ✅ Verified live                                                     |
| C4  | Sweep runs twice in one day                           | Second run sends nothing — `trial_ending_sent_at` guards it                                                                                                                             | ✅ Verified live                                                     |
| C5  | Warning email fails to send                           | Marker not stamped, so the next day's run retries. Verified by clearing the marker and re-running                                                                                       | ✅ Verified live                                                     |
| C6  | Venue converts before the warning is due              | Sweep skips it — re-checks status at send time, not at queue time                                                                                                                       | ✅ Handled                                                           |
| C7  | Venue cancels during the trial, warning still pending | Sweep skips it (`cancel_at_period_end` checked) — warning about a charge that will not happen would be false                                                                            | ✅ Handled                                                           |
| C8  | Trial ends with **no** payment method                 | `trial_settings.end_behavior.missing_payment_method=cancel` — Stripe cancels rather than invoicing someone who never agreed                                                             | ✅ Handled                                                           |
| C9  | Venue who already consumed a trial subscribes again   | `trialEndsAt` present ⇒ `trialDays = null` ⇒ **charged immediately**, one trial ever (D-42)                                                                                             | ✅ Verified live                                                     |
| C10 | Trial length changed in env                           | Affects **new** checkouts only; existing subscriptions keep the trial they were created with                                                                                            | ✅ Intended behavior                                                 |
| C11 | Trial extended in the Stripe Dashboard                | `trial_ends_at` is write-once (it is the D-42 marker), so we keep warning against the **original** date — early, never late                                                             | ⚠️ Discuss — documented trade-off, splitting the column would fix it |

---

## GROUP D — Renewal & Billing Cycles

| #   | Scenario                                                  | Expected Result                                                                                                                                                                                       | Status                                                               |
| --- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| D1  | Monthly subscription reaches period end                   | Auto-renews with no user action; second €19.99 paid; period rolls forward one month; status stays `active`                                                                                            | ✅ Verified live                                                     |
| D2  | Annual subscription reaches period end                    | Renews one year forward                                                                                                                                                                               | ✅ Handled (first annual charge verified; second cycle not advanced) |
| D3  | Successful renewal charge                                 | Payment-confirmation email quotes the **settled invoice amount**, never a local constant; "next renewal" matches `current_period_end`; manage link points at the app, not a stale Portal URL          | ✅ Verified live                                                     |
| D4  | €0 trial-opening invoice is paid                          | **No** confirmation email — guarded on `amount_paid > 0`. Confirmed after a real Checkout: the mailbox gained no receipt, so nobody gets "payment received — €0.00" six months before any money moves | ✅ Verified live                                                     |
| D5  | Same `invoice.paid` redelivered by Stripe                 | State is idempotent, but the **receipt email re-sends** — no processed-event table by design (D-22)                                                                                                   | ⚠️ Discuss — accepted cost; fix is storing the last paid invoice id  |
| D6  | Venue switches monthly → annual in the Portal             | Proration per the Portal configuration; our sync writes the new `plan` from the Price's `recurring.interval`                                                                                          | ❌ Not handled — Portal plan-switching not configured or tested      |
| D7  | Venue with no Stripe customer requests the billing Portal | `PRECONDITION_FAILED` with a user-facing message ("No subscription to manage yet — activate your profile first."), not a stack trace                                                                  | ✅ Verified live                                                     |

---

## GROUP E — Payment Failure & Dunning

| #   | Scenario                                          | Expected Result                                                                                                                                                                     | Status                                                                                                                     |
| --- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| E1  | Card fails at the first charge after trial        | Invoice `open`, `amount_paid: 0`, status → `past_due`, venue **stays visible**                                                                                                      | ✅ Verified live                                                                                                           |
| E2  | Stripe exhausts its retries                       | Stripe **cancels** the subscription → `customer.subscription.deleted` → status `cancelled` → venue hidden. The grace window is Stripe's schedule, not our arithmetic (D-33 revised) | ✅ Verified live                                                                                                           |
| E3  | Card recovers mid-retry                           | `invoice.paid` → status back to `active`, venue was visible throughout                                                                                                              | ✅ Handled                                                                                                                 |
| E4  | No payment-confirmation email for a failed charge | Nothing sent — `amount_paid` is 0                                                                                                                                                   | ✅ Verified live                                                                                                           |
| E5  | Venue is told its card failed                     | **Stripe's** dunning email does this; we deliberately no longer handle `invoice.payment_failed` (D-64)                                                                              | ❌ Not handled — all Stripe customer emails are currently **off**, so the venue is told nothing                            |
| E6  | Grace window equals the agreed 7 days             | Stripe cancels after 7 days                                                                                                                                                         | ❌ Not handled — the live-only setting reads **15 days**; our code path is identical either way, so no test can catch this |
| E7  | `invoice.payment_failed` arrives                  | Deliberately unhandled; `default: return`. Status still reaches us via `customer.subscription.updated`                                                                              | ✅ Verified live                                                                                                           |

---

## GROUP F — Cancellation

| #   | Scenario                                         | Expected Result                                                                                                               | Status           |
| --- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| F1  | Venue cancels at period end **during** the trial | Stays `trialing` and visible until `trial_end`, then `cancelled`. **Never charged** — only the €0 trial invoice exists (D-29) | ✅ Verified live |
| F2  | Venue cancels at period end while paying         | Keeps access to the end of the paid period, then hides                                                                        | ✅ Handled       |
| F3  | Subscription cancelled immediately               | Status `cancelled`, venue hidden at once                                                                                      | ✅ Verified live |
| F4  | Cancelled venue re-subscribes                    | Allowed, but **no second trial** (C9)                                                                                         | ✅ Verified live |
| F5  | Cancelled venue's existing events                | Its **own** events hide; artist-created events at that venue stay visible (V-06)                                              | ✅ Handled       |

---

## GROUP G — Chargeback & Billing Block

| #   | Scenario                                         | Expected Result                                                                                                                                                                                                              | Status                                                        |
| --- | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| G1  | Customer disputes a charge                       | `charge.dispute.created` → the customer is resolved **via the charge** (`charges.retrieve`) → `billing_blocked = true`, status `cancelled`, venue hidden                                                                     | ✅ Verified live                                              |
| G2  | Stripe cancellation fails during a dispute block | Error logged with an actionable message ("stays blocked locally but is still being billed. Cancel it by hand"); the local block still holds                                                                                  | ✅ Verified live                                              |
| G3  | A later `invoice.paid` re-syncs a blocked venue  | Status reverts to `active`, **but the venue stays hidden** — `venueVisibilityFor` checks `billing_blocked` before status. Observed for real: Stripe said `active`, our column said `active`, the public API served `on_hold` | ✅ Verified live                                              |
| G4  | Blocked venue tries to publish                   | `FORBIDDEN` — the guard reads the gate, not the status column                                                                                                                                                                | ✅ Verified live                                              |
| G5  | Blocked venue requests activation                | Refused (D-51)                                                                                                                                                                                                               | ✅ Handled                                                    |
| G6  | Blocked venue opens the app                      | Sees **nothing** explaining why it is hidden — no "under review" state exists                                                                                                                                                | ⚠️ Discuss — deferred with the chargeback admin screen (D-62) |

---

## GROUP H — Visibility Gate, per surface

| #   | Scenario                                        | Expected Result                                                                                                                                                                                                                                    | Status                                                                                                                                       |
| --- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| H1  | `VENUE_GATE_ENABLED=false` (shipping default)   | Every venue visible regardless of status; the gate issues **no DB query at all**. Verified by flipping the flag and restarting: the same on-hold venue went from `on_hold` with everything withheld to `visible` with phone and coordinates served | ✅ Verified live                                                                                                                             |
| H2  | `venues.byId` for a visible venue               | Full payload: bio, address, county, lat/lng, phone, websiteUrl, coverImageUrl                                                                                                                                                                      | ✅ Verified live                                                                                                                             |
| H3  | `venues.byId` for an on-hold venue              | `visibility: on_hold`; **all eight** content fields withheld; `displayName` still served so the client can name which venue lapsed (D-52)                                                                                                          | ✅ Verified live                                                                                                                             |
| H4  | On-hold venue resolved by handle                | Resolves to `on_hold`, never 404 — "hidden" must not read as "does not exist"                                                                                                                                                                      | ✅ Handled                                                                                                                                   |
| H5  | `events.byId` for an on-hold venue's event      | `venueOnHold: true`; description, ticket link/price and ad copy withheld; title and date kept so the card stays recognisable                                                                                                                       | ✅ Verified live                                                                                                                             |
| H6  | `events.byId` for a visible venue's event       | `venueOnHold: false`, description served                                                                                                                                                                                                           | ✅ Verified live                                                                                                                             |
| H7  | Creator opens their **own** event while on hold | Full detail — they need it to fix payment                                                                                                                                                                                                          | ✅ Handled                                                                                                                                   |
| H8  | On-hold venue's own events on map and feed      | Dropped (V-03), post-filtered on `creator_id` rather than indexed in Typesense (D-54). **Controlled test:** gate off → map 2 pins / feed 1, event present; gate on → map 1 / feed 0, event excluded                                                | ✅ Verified live                                                                                                                             |
| H9  | Artist-created event at an on-hold venue        | **Stays visible** (V-06). Verified with both events pointing at the _same_ on-hold venue in one map query: venue-created excluded, artist-created present. Hiding depends on who created it, not which venue it names                              | ✅ Verified live                                                                                                                             |
| H10 | On-hold venue's posts                           | Stay visible (V-07)                                                                                                                                                                                                                                | ✅ Handled                                                                                                                                   |
| H11 | Artist's venue picker                           | `venues.list` returns all 47 venues with 43 flagged `onHold: true` — **listed and flagged, not filtered out** (V-09), so the client can badge them. The unselectable rendering itself is client-side (K9)                                          | ✅ Verified live (server side)                                                                                                               |
| H12 | Saved event whose venue went on hold            | Both saved events returned — **kept, not filtered**. The on-hold venue's own event carries `venueOnHold: true`; an artist's event at the _same_ on-hold venue carries `false`, so V-06 holds inside the saved list too (D-52)                      | ✅ Verified live                                                                                                                             |
| H13 | Feed ads from an on-hold venue                  | Withdrawn (V-11)                                                                                                                                                                                                                                   | ❌ Not handled — no ad-bearing events exist locally, so the query returns 0 either way and proves nothing                                    |
| H14 | Collections owned by an on-hold venue           | Hidden, as are the events inside them (D-59)                                                                                                                                                                                                       | ❌ Not handled — `collections.list` authenticates and returns 0 because no collections exist locally, so the query proves nothing either way |
| H15 | Admin venue detail                              | `isActive` reads the real gate, not a status inference — so it cannot report the inverse of what a spectator sees                                                                                                                                  | ✅ Handled                                                                                                                                   |
| H16 | Never-subscribed venue with no billing row      | Still gated — the gate LEFT-joins, so a missing row is not read as "fine"                                                                                                                                                                          | ✅ Verified live (unit)                                                                                                                      |

---

## GROUP I — Race Conditions & Idempotency

| #   | Scenario                                         | Expected Result                                                                                                                                                | Status                                                 |
| --- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| I1  | Two concurrent first-time webhooks for one venue | Upsert on the unique `venue_id` — neither dies on a unique violation. Stripe fans out `subscription.created` and `invoice.paid` at nearly the same instant     | ✅ Handled                                             |
| I2  | Webhook redelivered after success                | Re-fetches from Stripe and rewrites the same state; no duplicate row                                                                                           | ✅ Verified live                                       |
| I3  | Webhook arrives out of order                     | Handler re-fetches from Stripe rather than trusting the payload, so the latest truth wins for state — though a late-arriving fetch can still commit stale data | ⚠️ Discuss — no processed-event table by design (D-22) |
| I4  | Forged or absent webhook signature               | 400, body never logged, nothing processed                                                                                                                      | ✅ Verified live                                       |
| I5  | Handler throws mid-processing                    | 500 so Stripe retries                                                                                                                                          | ✅ Handled                                             |
| I6  | Unknown Stripe status arrives                    | Logged loudly, **nothing written** — a future status is never coerced into a visibility decision                                                               | ✅ Handled                                             |
| I7  | Subscription with no resolvable venue            | Logged, state untouched. This is why `stripe trigger` no-ops: it fabricates its own customer                                                                   | ✅ Verified live                                       |
| I8  | Double-tap on Activate Profile                   | Second request rate-limited; the client converts `TOO_MANY_REQUESTS` into a cooldown, not an error                                                             | ✅ Handled                                             |
| I9  | Portal link requested repeatedly                 | Cooldown stamped **before** the billable Stripe call, so a failure cannot leave the throttle un-armed and mint unbounded sessions                              | ✅ Handled                                             |

---

## GROUP J — Account Deletion & GDPR

| #   | Scenario                                         | Expected Result                                                                                                                                                                        | Status     |
| --- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| J1  | Venue requests deletion with a live subscription | Billing cancelled at Stripe **before** erasure; if Stripe is unreachable the job retries rather than erasing an account that keeps being charged (D-47)                                | ✅ Handled |
| J2  | Erasure runs                                     | `activation_tokens` explicitly deleted — the FK cascades on user _delete_, but erasure anonymises in place, so the cascade never fires and a live credential would outlive the account | ✅ Handled |
| J3  | Erasure runs for a venue                         | `stripe_customer_id` and `stripe_subscription_id` nulled — Stripe holds the billing name and address against that id, so leaving it is not erasure                                     | ✅ Handled |
| J4  | Erasure for a non-venue account                  | No billing write attempted                                                                                                                                                             | ✅ Handled |
| J5  | Anonymised venue's visibility                    | Moved to `cancelled`, so a deleted venue cannot remain visible mid-trial                                                                                                               | ✅ Handled |

---

## GROUP K — Native App Surfaces

> **App run performed 18/08/2026.** The app was launched in the iOS 17 Pro simulator from a
> `com.raftlabs.ceolx.staging` dev client, bundling from local Metro with
> `.env.development.local` loaded first so `EXPO_PUBLIC_API_BASE_URL=http://localhost:3001`
> — the app talked to the **local** backend, never staging. The login screen rendered and the
> local server logged its auth traffic, so the app↔backend path is proven.
>
> **The UI could not be driven further.** AppleScript clicks do not reach the simulator's
> Metal-rendered view, and RN text inputs drop keystrokes under Tab focus, so sign-in could not
> be completed programmatically. Every row below therefore separates the **data source**
> (verified through a real authenticated session against the local server) from the
> **rendering** (not verified). Closing these properly needs an e2e harness — Maestro or
> Detox — which this repo does not have, or a human with the simulator in front of them.

| #   | Scenario                                                                                  | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Status                                                                                                         |
| --- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| K1  | Venue with a **hidden** profile opens its profile                                         | "Your profile isn't live yet" + lock icon + **ACTIVATE PROFILE**. No price, no URL, no checkout button anywhere (D-16). Card spans full width, confirming the `self-stretch` fix. **Screenshotted in the simulator against the local backend**                                                                                                                                                                                                                                                 | ✅ Verified live                                                                                               |
| K2  | **Grandfathered** venue (unsubscribed but still visible) opens its profile                | "**Action needed to keep your profile live**" — "your profile is live and artists can find you…". Never claims the profile is dead, because it is not. Previously showed nothing at all, so the venue would have been hidden at cutover without warning. **Screenshotted with the gate off**                                                                                                                                                                                                   | ✅ Verified live                                                                                               |
| K3  | Trialing venue opens its profile                                                          | A small **TRIAL PERIOD** badge with an info glyph sits under the venue name — no card, no paragraph. Tapping it opens a native dialog: "Your free trial runs until 17 February 2027. We'll email you before the first payment — there's nothing you need to do until then." Renders without a date too (seeded venues have no billing row). **The paragraph form was screenshotted on an Android device after a real payment; the badge form replaced it on 18/08/2026 and needs a re-check.** | ⚠️ Discuss — date verified live, badge rendering not                                                           |
| K4  | Past-due venue                                                                            | Fix-payment banner, both inside and past the retry window                                                                                                                                                                                                                                                                                                                                                                                                                                      | ❌ Not handled — never exercised on a device                                                                   |
| K5  | Blocked venue opens the post composer                                                     | Publish disabled and visibly dim (`bg-[#6155F5]/50` — measured at rgb(52,48,125) on device, vs rgb(48,42,122) expected), and `handlePublish` returns **before uploading** so no S3/Mux media is orphaned. The notice reads "…Open your profile to activate" and is **tappable**, replacing the composer with the profile. Data source verified: `users.me` gives `mayPublish: false`; the server also refuses via `assertVenueMayPublish`.                                                     | ✅ Verified live — dim state and copy screenshotted on device 18/08/2026; the tap target is new and unverified |
| K6  | Blocked venue opens the event form                                                        | Same notice on the final step, tappable, and CREATE EVENT disabled. Server-side refusal verified live (`FORBIDDEN` carrying the shared message).                                                                                                                                                                                                                                                                                                                                               | ⚠️ Discuss — server guard verified, rendering and tap target not                                               |
| K7  | Resend cooldown, app backgrounded 5 minutes                                               | Countdown tracks an absolute deadline, so it does not read "Resend in 43s" after the server cooldown expired                                                                                                                                                                                                                                                                                                                                                                                   | ❌ Not handled — never exercised on a device                                                                   |
| K8  | Leave the profile tab mid-cooldown and return                                             | Cooldown and "check your inbox" copy survive (module-scoped deadline)                                                                                                                                                                                                                                                                                                                                                                                                                          | ❌ Not handled — never exercised on a device                                                                   |
| K9  | Artist picks an on-hold venue                                                             | Row listed, badged, unselectable, address readable. Data source verified live: `venues.list` returns all 47 with 43 flagged `onHold: true` — listed, not filtered                                                                                                                                                                                                                                                                                                                              | ⚠️ Discuss — data verified, rendering not                                                                      |
| K10 | Event detail for an on-hold venue's event                                                 | "Details are being confirmed by the venue" instead of blank fields. Data source verified live: `events.byId` returns `venueOnHold: true` with description, ticket and ad withheld                                                                                                                                                                                                                                                                                                              | ⚠️ Discuss — data verified, rendering not                                                                      |
| K11 | Activation funnel analytics                                                               | `venue_activation_prompt_shown` on mount, `venue_activation_email_requested` only on success, `venue_publish_blocked` with surface                                                                                                                                                                                                                                                                                                                                                             | ❌ Not handled — needs `EXPO_PUBLIC_POSTHOG_DEV_CAPTURE=true`                                                  |
| K12 | New venue completes onboarding (taps CREATE PROFILE on step 3)                            | Lands on the **activation hand-off screen**, not the map. Reads "One last step" + the account email + OPEN EMAIL APP. Previously landed on the map with nothing said and **no email sent at all** — the requirement was discoverable only by opening their own profile later.                                                                                                                                                                                                                  | ❌ Not handled — new 18/08/2026, needs a device run                                                            |
| K13 | The hand-off screen's claim that an email was sent                                        | Actually sent: the screen fires `venues.requestActivation` on mount and holds a spinner ("Setting up your subscription…") until it resolves, so the sentence is never rendered optimistically                                                                                                                                                                                                                                                                                                  | ❌ Not handled — new, needs a device run                                                                       |
| K14 | Activation email fails to send on that screen                                             | Shows "We couldn't send the activation email just now" + the server message + **TRY AGAIN**. Never the success copy.                                                                                                                                                                                                                                                                                                                                                                           | ❌ Not handled — force by stopping Postmark/Mailpit                                                            |
| K15 | Screen remounts (fast refresh, or app resumed) while the first email is still in cooldown | The mount send is ref-guarded, and a `TOO_MANY_REQUESTS` is read as "already sent" rather than an error — so the venue is never told sending failed when it succeeded, and the first token is never revoked underneath them (D-18)                                                                                                                                                                                                                                                             | ❌ Not handled — new, needs a device run                                                                       |
| K16 | Close the hand-off screen (X, or Android back)                                            | Goes to the map. Android hardware back must **not** exit the app — onboarding replaced its way here, so there is nothing to pop to. The muted line "No rush — you can finish this any time from your profile" is what makes closing safe.                                                                                                                                                                                                                                                      | ❌ Not handled — new, needs a device run                                                                       |
| K17 | Reopen the app after closing the hand-off screen                                          | It does not come back — it is reached only from the onboarding submit. The profile prompt (K1/K2) is the recovery surface, and the three emailed reminders (D-26) are the other.                                                                                                                                                                                                                                                                                                               | ❌ Not handled — new, needs a device run                                                                       |
| K19 | Pay in a browser, then return to the still-open hand-off screen                           | Flips to **"You're all set"** with the trial end date and a CONTINUE button. The screen polls `users.me` every 10s until the venue has live billing, then stops. **Regression found on a device 18/08/2026:** the first version polled only on the profile card, so a venue who had paid came back to "One last step" with no way to tell it had worked — the webhook had landed 96 seconds earlier.                                                                                           | ❌ Not handled — fix written, needs the device re-run                                                          |
| K18 | Tap the publish-blocked notice                                                            | Lands on the profile, where **ACTIVATE PROFILE** is. Replaces the composer rather than pushing, so Back does not return to a screen that still refuses them. The old copy sent them to their inbox — the one place a venue seeing this notice has already failed to get a link from.                                                                                                                                                                                                           | ❌ Not handled — new 18/08/2026, needs a device run                                                            |

---

## Needs Discussion Before Deployment

| #   | Issue                                                                                                            | Impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| X1  | **Stripe's retry schedule is 15 days, not the 7 D-33 specifies**, and it is a live-mode-only setting             | The grace window _is_ this setting now. An unpaid venue stays visible for two weeks instead of one, and **no test can detect it** — Stripe exposes no API to read it back. A human must confirm the field in live mode.                                                                                                                                                                                                                                                        |
| X2  | **Every Stripe customer email is off**, and we deliberately dropped `invoice.payment_failed`                     | A venue whose card fails is told nothing at all: it goes quiet and is cancelled N days later. Also live-mode-only, so the wording cannot be reviewed before launch.                                                                                                                                                                                                                                                                                                            |
| X3  | ~~O-08 — what existing venues get at cutover~~ — **CLOSED 18/08/2026 as D-67**                                   | Resolved by deciding _not_ to migrate data. **Production, read-only check 18/08/2026:** 13 venue accounts, 10 with a profile (3 never finished onboarding). **All 10 are `subscription_status = 'inactive'`, `is_active = false`, no `stripe_customer_id`, and `venue_subscriptions` has 0 rows.** The M8 migration is not deployed — the production enum is still `inactive                                                                                                   | active | past_due | cancelled`with no`trialing`, and `venue_profiles`has no`trial_ends_at`. Totals: 123 artists, 95 spectators, 13 venues, 1 admin. With the gate off — the shipping default — every one of those 10 renders `activate_grace`("Action needed to keep your profile live") and keeps`mayPublish: true`, which is verified live below. Each is emailed by hand and gets the full 183-day trial from their own activation. This deletes the whole Option A build — `has_payment_method`, a "trialing but no card" client state, and a migration script with a floor for venues older than 183 days. One operational consequence: **the manual email cannot contain an activation link**, because tokens live 45 minutes (D-17). It has to send them to the app's **Activate profile** button. |
| X4  | **No VAT is collected**                                                                                          | `automatic_tax` is off and Chongie has no confirmed Irish registration. €19.99 is deemed VAT-inclusive (verified: both Prices carry `tax_behavior: inclusive`), so if they are registered the VAT is owed out of money already banked (~€3.74/month/venue). Accountant question, not an engineering one. Related but now closed: Adaptive Pricing was silently converting the charge to local currency with a 4% fee — see D-68.                                               |
| X5  | **A chargeback-blocked venue is hidden and told nothing**                                                        | No "under review, contact us" state exists (D-62 deferred). Silence beats a button that errors, but it is not right.                                                                                                                                                                                                                                                                                                                                                           |
| X6  | ~~The Checkout card form has never been exercised~~ — **CLOSED 18/08/2026**                                      | A real card payment was completed on an Android device against the local backend: `/activate` → Stripe payment sheet → `/activate/complete` → webhook → `trialing` in the database → trial notice in the app. The creation path is now proven end to end.                                                                                                                                                                                                                      |
| X7  | **The client surface is verified at the data layer but never rendered**                                          | The app was run in the simulator against the local backend on 18/08/2026 and the app↔backend path is proven, but the UI could not be driven programmatically: AppleScript clicks do not reach the simulator's Metal view and RN inputs drop keystrokes under Tab focus. Every K row's data source is verified through a real authenticated session; none of the _rendering_ is. **This wants an e2e harness (Maestro or Detox) — the repo has none — or a manual pass by QA.** |
| X8  | **Ads and collections gating (H13, H14) untested**                                                               | Map, feed and the venue picker are now verified live with a gate-on/gate-off control. Ads return 0 locally because no ad-bearing events exist, and collections require auth. Both are post-filtered in application code (D-54), where a missing filter is invisible to mocked tests — precisely the risk the design accepted.                                                                                                                                                  |
| X9  | **Redelivered `invoice.paid` re-sends the receipt**                                                              | Deliberate: there is no processed-event table (D-22). A duplicate receipt is the accepted cost; storing the last paid invoice id would fix it.                                                                                                                                                                                                                                                                                                                                 |
| X10 | **`past_due_since`, `venue_subscriptions.status` and `venue_profiles.is_active` are deprecated but not dropped** | Expand/contract — a follow-up PR must drop them once this release is fully live, or they rot as three columns nothing reads.                                                                                                                                                                                                                                                                                                                                                   |
