# Handoff memory triage (scratch file — deleted in Task 6)

Source: `~/.claude/projects/-Users-priyayadav-Documents-Raftlabs-CeolX/memory/` (73 files, `MEMORY.md` excluded — 72 read in full).

Selection test (keep only if ≥1 true): blocks/breaks build-deploy-release · causes silent data loss / wrong-data · is a platform trap costing a fluent dev a day · encodes a non-obvious architectural rule hard to detect.

Format: `slug — one-line symptom — source memory filename`
A `[also X]` suffix means the same memory feeds more than one target file; it is listed once per target category.

---

## gotcha-auth
(→ 01-gotchas.md § Auth & onboarding)

- deeplink_bridge_double_fire — reset-password/verify-email deep link bounces to sign-in because the bridge fires `ceolx://` twice (meta-refresh + JS), re-anchoring the auth splash on Android `onNewIntent` — project_deeplink_bridge_double_fire.md
- deeplink_bridge_parse_halt — verify-email desktop fallback never runs: `location.replace()` mid-parse halts every later inline `<script>`, so a fallback script must be armed before the redirect fires — project_deeplink_bridge_parse_halt.md
- email_normalization_plus_addressing — "verify again" bug was email casing, not +tag stripping; BetterAuth needs a lowercase+trim normalize hook, `+tag` must survive (independent accounts by design) — project_email_normalization_plus_addressing.md
- get_started_onboarding_keychain_gate — `hasSeenOnboarding` lives in SecureStore/Keychain, survives app uninstall, so device reinstall doesn't reliably show Get Started again — project_get_started_onboarding_keychain_gate.md
- guest_flag_via_auth_context — guest "Skip" must call `continueAsGuest()` (state+SecureStore together), never a bare SecureStore write; any unguarded `useMe()` call poisons the guest query cache — project_guest_flag_via_auth_context.md
- location_setup_flag_per_user — location-setup-complete flag was device-global, so a new user on a device with a prior completed account skipped the location screen entirely — project_location_setup_flag_per_user.md
- logout_stale_role_cache — logout must call `queryClient.clear()`; a disabled `useMe()` still returns the prior user's cached `currentRole`, so a guest can see the previous artist/venue view — project_logout_stale_role_cache.md
- social_signup_role_mechanism — Google/Apple signup can't pass `currentRole` through OAuth; role is only applied by a one-shot `completeRegistration` replay, with a `pendingHandled` ref bug across logout — project_social_signup_role_mechanism.md
- open_email_app_android_intent — [also gotcha-build] "Open Email App" needs an Android intent fallback chain (`CATEGORY_APP_EMAIL` isn't universally declared); native change, bounced 3 weeks because fixes shipped OTA — project_open_email_app_android_intent.md

## gotcha-map
(→ 01-gotchas.md § Map & discovery)

- android_marker_onpress — Android flattens a `<Marker>`'s custom child to a bitmap; the tap handler must live on `<Marker onPress>`, never an inner `<Pressable>` (works on iOS, dead on Android) — project_android_marker_onpress.md
- map_marker_fabric_clustering_blank — blank pin images = Fabric (newArch) + pre-Fabric `react-native-map-clustering@4` remount churn throwing `addViewAt` mount errors, NOT an image-timing bug — project_map_marker_fabric_clustering_blank.md
- map_pin_tracksviewchanges — `tracksViewChanges` freezing before the remote image paints causes slow/blank custom map pins; must settle per-marker after image `onLoad` (deferred) — project_map_pin_tracksviewchanges.md
- discovery_map_read_from_typesense — [also arch] Discovery feed + Map read from Typesense, not Neon directly; coordless events were coerced to `(0,0)` and silently vanished — project_discovery_map_read_from_typesense.md
- discovery_search_is_global — [also arch] default browse caps at 100km but a text query drops the geo filter entirely, so search is nationwide by design — project_discovery_search_is_global.md
- ip_fallback_vercel_geo_headers — IP-location fallback was flaky because ipapi.co free tier rate-limits the shared server egress IP; prefer Vercel's `x-vercel-ip-*` geo headers first — project_ip_fallback_vercel_geo_headers.md

## gotcha-media
(→ 01-gotchas.md § Media/posts/feed)

- delete_post_tombstone — deleting a post stayed visible on feed/profile until restart; accumulated list hooks shadow the query cache and can't express removals via merge, fixed with a shared tombstone Set — project_delete_post_tombstone.md
- media_upload_stream_not_blob — video/image upload must stream from the file URI via `expo-file-system` `BINARY_CONTENT`, not `fetch().blob()`, which OOMs on Android for large files — project_media_upload_stream_not_blob.md
- reels_video_playback — discover Posts tab needs its own `FeedPostsList` FlatList (viewport-gated autoplay) because converting the shared `.map()`-based `PostsList` would trip nested-VirtualizedList breakage — project_reels_video_playback.md

## gotcha-notif
(→ 01-gotchas.md § Notifications & deep-linking)

- notification_deeplink_routing — [also arch] `routeTemplate` must use fully-qualified Expo Router group paths (`/(app)/(tabs)/...`); bare paths 404 via `+not-found` — project_notification_deeplink_routing.md
- notification_copy_in_shared — [also arch] notification title/body/persona/route must come from typed builders in `@CeolX/shared`, never inlined in routers, or push/email copy drifts from the PM-audited matrix — feedback_notification_copy_in_shared.md

## gotcha-events
(→ 01-gotchas.md § Events & bookings)

- accumulated_list_stale_edit — event list hooks (my-events/saved/confirmed/feed) shadow the React Query cache; an in-place edit that doesn't change list length/first-id stays stale until restart — use `mergePaginatedEvents` — project_accumulated_list_stale_edit.md
- archived_status_means_deleted — [also arch] `status='archived'` uniquely means creator-deleted (no auto-archive cron); every new event-listing query must filter it out or deleted events leak — project_archived_status_means_deleted.md
- collections_upcoming_only — Collections must exclude both past (`active` + elapsed date) and deleted (`archived`) events; filtered in JS in `byId` (test-mock constraint) but in SQL for the 5-row `relatedEvents` preview — project_collections_upcoming_only.md
- edit_clear_field_null_not_undefined — clearing an optional field on Edit Event silently didn't persist: `undefined` means "don't touch" end-to-end, so clearing requires `null` + `.nullable()` schema + client sending `null` — project_edit_clear_field_null_not_undefined.md
- event_form_validation_routing — [also arch] cross-field rules must exist in BOTH the per-step validation system and the shared Zod schema, and submit errors must route to the step that owns the field — project_event_form_validation_routing.md
- past_event_accept_guard — past events stay `status='active'`; booking ACCEPT/apply/resend need an explicit `isEventPast` guard, and API test fixtures with fixed past dates require pinning the clock — project_past_event_accept_guard.md
- save_toggle_byid_cache_sync — [also arch] any toggle mutation (save/join/follow) must patch the `events.byId` detail cache, not just list caches; detail views must derive state from the query, never snapshot into `useState` — project_save_toggle_byid_cache_sync.md
- follow_count_vs_list_filter — [also gotcha-db] Following count used a raw `count(*)` while the list filtered to active-public-non-self profiles; mock-DB unit tests can't catch SQL `WHERE` divergence — project_follow_count_vs_list_filter.md
- artist_name_model — artist search now matches `stage_name` OR account name and is `protectedProcedure` (was public, exposed personal data) — project_artist_name_model.md
- venue_location_is_map_pin — [also arch] venue location is a mandatory map pin (lat/lng) via the shared `LocationPicker`, never a free-text address field — project_venue_location_is_map_pin.md
- profile_image_two_columns — [also arch] avatars have two source columns (`user.image` OAuth-only vs profile `profileImageUrl`); events/bookings routers repeatedly read the wrong one — project_profile_image_two_columns.md
- kav_height_modal_flicker — `KeyboardAvoidingView behavior="height"` inside an RN Modal double-corrects with Android `adjustResize` and flickers; use `"padding"` on both platforms, no `autoFocus` — project_kav_height_modal_flicker.md

## gotcha-email
(→ 01-gotchas.md § Email)

- email_matrix_scope_gap — [also state] most matrix emails (booking invites, GDPR, subscription-failure) were never implemented — M7-T0 specced 38, M7-T3 built only 6; two distinct dispatch paths (auth-sync vs QStash) — project_email_matrix_scope_gap.md
- invite_email_orphaned_procedure — [also state] external artist invite email was built on `bookings.inviteExternal`, a procedure with zero frontend callers; the real path is `events.create/update`'s `unregisteredCollaborators` — project_invite_email_orphaned_procedure.md

## gotcha-build
(→ 01-gotchas.md § Builds & EAS)

- eas_fingerprint_node_compat — `apps/native/fingerprint.config.cjs` MUST be `.cjs` (not `.js`) or `@expo/fingerprint`'s `require()` silently swallows `ERR_REQUIRE_ESM` and the version-skip config becomes `{}` — feedback_eas_fingerprint_node_compat.md
- pnpm_install_after_lockfile_change — [also runbook] a fingerprint mismatch with no `app.config.js` changes usually means stale local `node_modules`; run `pnpm install --frozen-lockfile` to match what EAS sees — feedback_pnpm_install_after_lockfile_change.md
- app_config_fcm_lines_commented — Firebase Swift pods (`FirebaseCoreInternal`→`GoogleUtilities`) break iOS `pod install` unless `use_modular_headers!` is injected via a `withDangerousMod` plugin — project_app_config_fcm_lines_commented.md
- eas_env_loading — [also runbook] EAS Cloud builds never see local `.env.*` files (gitignored + `NODE_ENV=production` for all profiles); `EXPO_PUBLIC_*` vars must come from `eas.json`/EAS dashboard — project_eas_env_loading.md
- eas_node_version_pnpm_floor — [also runbook] bumping the `packageManager` pnpm version silently requires a Node floor; pin `node` per build profile in `eas.json` or the build fails before evaluating the lockfile — project_eas_node_version_pnpm_floor.md
- env_core_runtimeenv_gotcha — `@t3-oss/env-core`'s `runtimeEnv` must enumerate each `EXPO_PUBLIC_*` key statically; passing `process.env` directly leaves every value `undefined` in the production Metro bundle — project_env_core_runtimeenv_gotcha.md
- metro_pnpm_monorepo_config — `metro.config.cjs` must keep `EXPO_NO_METRO_WORKSPACE_ROOT` pinning serverRoot to projectRoot, or the eager bundle step crashes with "Unexpectedly escaped traversal" — project_metro_pnpm_monorepo_config.md
- add_to_calendar_native_rebuild — "Add to Calendar" moved to native `expo-calendar`; ships only via a fresh EAS build, never via OTA `eas update` — project_add_to_calendar_native_rebuild.md
- open_email_app_android_intent — [also gotcha-auth] native intent fix can't ship OTA — see gotcha-auth entry above — project_open_email_app_android_intent.md
- pr55_fcm_revert — [also state] iOS Firebase Swift-pod conflict fix is `ios.buildReactNativeFromSource: true`; the proposed FCM revert branch was never merged — FCM stack is fully active on development — project_pr55_fcm_revert.md
- server_typecheck_gate_gotcha — [also runbook] `server#check-types` is a pre-existing false-negative (hono/jsx TS2875); the real gate is `turbo build`, and `tsc -b`'s incremental cache can mask real exhaustiveness errors — project_server_typecheck_gate_gotcha.md
- local_staging_build_validation — [also runbook] validating a staging build locally needs `-scheme CeolXStaging` explicitly — `xcodebuild -list`'s alphabetical `schemes[0]` is `AppAuth`, a false-positive green build — project_local_staging_build_validation.md

## gotcha-deploy
(→ 01-gotchas.md § Deploy & infra)

- hono_vercel_export_pattern — [also runbook] Vercel Hono entry must `export default app` directly; `handle()` from `hono/vercel` forces the legacy Node runtime, which replaces `Request.url` on every rewrite and 404s every route but the rewrite target — feedback_hono_vercel_export_pattern.md
- envx_gpg_merge_conflict — [also runbook] `.env.*.gpg` always conflicts (non-deterministic encryption); resolve by re-encrypting local plaintext, never merging ciphertext, and key-diff both sides to catch silently dropped vars — project_envx_gpg_merge_conflict.md
- vercel_watches_client_remote — [also runbook] Vercel is only connected to the `client` remote; pushing `staging`/`main` to `raftlabs` alone triggers no deployment — project_vercel_watches_client_remote.md
- staging_android_apk_buildtype — [also runbook] Play requires `app-bundle` not `apk`; "version code already used" means `versionCode` didn't autoIncrement, not a build-format problem — project_staging_android_apk_buildtype.md
- play_app_signing_sha_checklist — [also runbook] Google Play re-signs the uploaded bundle; Maps/Sign-In must authorize against the Play App Signing SHA (Play Console only), not the EAS upload key — project_play_app_signing_sha_checklist.md
- db_migrate_ci_neon_staging — [also runbook, gotcha-db] staging DB-migrate GitHub Action: never run `db:migrate` through turbo (marked `persistent`, hangs CI); use `migrate` not `push`; new workflows must also land on the default branch (`main`) to appear in the Actions tab — project_db_migrate_ci_neon_staging.md
- shared_post_deeplink — [also arch, runbook] shared post deep links depend on `api.ceolx.ie` being the live prod custom domain, the correct per-env Android SHA-256, and Vercel deployment protection being off — project_shared_post_deeplink.md
- qstash_delay_cap_use_cron_sweep — [also runbook] QStash free plan caps delayed publish at 7 days; a 30-day GDPR erasure job must use a daily cron sweep over a DB timestamp, not a single delayed publish — project_qstash_delay_cap_use_cron_sweep.md

## gotcha-db
(→ 01-gotchas.md § DB & data-modeling)

- neon_fresh_db_push_not_migrate — [also runbook] `drizzle-kit migrate` from an empty Neon branch is broken (migration 0002 drops a constraint that was already cascade-dropped); use `push --force` on the direct endpoint + stamp `__drizzle_migrations` — project_neon_fresh_db_push_not_migrate.md
- follow_count_vs_list_filter — see gotcha-events entry above (same memory, filed under both) — project_follow_count_vs_list_filter.md
- db_migrate_ci_neon_staging — see gotcha-deploy entry above (same memory, filed under both) — project_db_migrate_ci_neon_staging.md

## state
(→ 02-project-state.md)

- email_matrix_scope_gap — only 6 of 38 specced V1 emails are built; email-only rows (payment-failed, GDPR) must not go through `dispatchNotification` (always writes inbox); remaining scope tracked in `docs/project-management/M7-Notifications-Emails/M7-T4-Remaining-Matrix-Emails.md` — project_email_matrix_scope_gap.md
- invite_email_orphaned_procedure — flag: a "built and tested" feature can still be dead if the UI calls a different procedure than the one carrying the logic; `bookings.inviteExternal` remains unused (tested but no callers) — project_invite_email_orphaned_procedure.md
- pr55_fcm_revert — corrects a stale assumption: the FCM push stack is fully live on `development`/production, not reverted; don't reintroduce a revert based on the unmerged `fix/m7-t1-fcm-revert` branch — project_pr55_fcm_revert.md

## runbook
(→ 03-ops-runbooks.md)

- eas_update_ota_staging — exact OTA publish command for the staging channel; `APP_VARIANT=staging` and `EXPO_PUBLIC_SHARE_BASE_URL` are BOTH mandatory or the OTA silently orphans; verify with `eas fingerprint:compare` before publishing — project_eas_update_ota_staging.md
- eas_env_loading — see gotcha-build entry above — project_eas_env_loading.md
- eas_node_version_pnpm_floor — see gotcha-build entry above; includes the brotli-decode recipe for `eas build:view --json` logs — project_eas_node_version_pnpm_floor.md
- pnpm_install_after_lockfile_change — see gotcha-build entry above — feedback_pnpm_install_after_lockfile_change.md
- hono_vercel_export_pattern — see gotcha-deploy entry above — feedback_hono_vercel_export_pattern.md
- neon_fresh_db_push_not_migrate — see gotcha-db entry above — project_neon_fresh_db_push_not_migrate.md
- db_migrate_ci_neon_staging — see gotcha-deploy/gotcha-db entries above — project_db_migrate_ci_neon_staging.md
- envx_gpg_merge_conflict — see gotcha-deploy entry above — project_envx_gpg_merge_conflict.md
- vercel_watches_client_remote — see gotcha-deploy entry above — project_vercel_watches_client_remote.md
- typesense_cloud_in_prod — production search is managed Typesense Cloud (<$20/mo); docker-compose Typesense is local-dev-only — project_typesense_cloud_in_prod.md
- staging_android_apk_buildtype — see gotcha-deploy entry above — project_staging_android_apk_buildtype.md
- play_app_signing_sha_checklist — see gotcha-deploy entry above — project_play_app_signing_sha_checklist.md
- local_staging_build_validation — see gotcha-build entry above — project_local_staging_build_validation.md
- server_typecheck_gate_gotcha — see gotcha-build entry above; includes the `--filter=server` (not `@CeolX/server`) turbo gotcha — project_server_typecheck_gate_gotcha.md
- qstash_delay_cap_use_cron_sweep — see gotcha-deploy entry above — project_qstash_delay_cap_use_cron_sweep.md
- shared_post_deeplink — see gotcha-deploy entry above — project_shared_post_deeplink.md

## arch
(→ 04-architecture.md)

- discovery_map_read_from_typesense — see gotcha-map entry above; also documents the recovery path (`admin.resyncEvents`) for a dead Typesense cluster — project_discovery_map_read_from_typesense.md
- discovery_search_is_global — see gotcha-map entry above — project_discovery_search_is_global.md
- notification_copy_in_shared — see gotcha-notif entry above — feedback_notification_copy_in_shared.md
- notification_deeplink_routing — see gotcha-notif entry above — project_notification_deeplink_routing.md
- notification_schema_split — `notifications` (content) + `notification_users` (per-user is_read/archived_at) is a normalised two-table schema; mark-read/delete mutations target `notification_users` only — project_notification_schema_split.md
- archived_status_means_deleted — see gotcha-events entry above — project_archived_status_means_deleted.md
- profile_image_two_columns — see gotcha-events entry above — project_profile_image_two_columns.md
- venue_location_is_map_pin — see gotcha-events entry above — project_venue_location_is_map_pin.md
- save_toggle_byid_cache_sync — see gotcha-events entry above — project_save_toggle_byid_cache_sync.md
- event_form_validation_routing — see gotcha-events entry above — project_event_form_validation_routing.md
- shared_post_deeplink — see gotcha-deploy entry above; domain layout: `ceolx.ie` root = apps/admin (Vite SPA), `api.ceolx.ie` = apps/server (Hono), both `/post/*` and `/.well-known/*` rewritten (not redirected) to the server — project_shared_post_deeplink.md

---

## Dropped (did not meet the selection test)

- feedback_aravind_not_boss — people-routing preference, not a build/data/platform/arch trap; Task 6 sources "key people" directly from CLAUDE.md instead
- feedback_branch_name_pattern — pure process reminder already enforced by the pre-push hook / commitlint; becomes one README rules-table line, not a gotcha entry
- feedback_check_commitlint_before_commit — same as above (commit message format, already enforced by tooling)
- feedback_code_review_graph — process reminder already stated in CLAUDE.md's "Code Navigation" section
- feedback_fetch_after_gh_pr_merge — process reminder (git workflow hygiene), no build/data/platform/arch impact
- feedback_form_validation_ux_ontouched — UX preference (validate onTouched), not build-blocking/data-loss/platform-trap/hard-to-detect-architecture
- feedback_no_stylesheet_create — styling convention; becomes a README rules-table line ("never StyleSheet.create"), not a gotcha entry
- feedback_pr_base_is_development — pure process reminder (PR base branch), already a README rules-table line
- feedback_pr_rebase_and_merge — pure process reminder (merge strategy), pairs with the above
- project_android_calendar_sync_not_selection — single-ticket misdiagnosis note (OS sync adapter, not app bug); narrow scope, low recurrence risk
- project_collaborator_invite_design — superseded by the 31/05/2026 Invite-Artist consolidation; current state is already fully captured in CLAUDE.md's "Event form field distinctions" section
- project_event_form_business_rules — same content as CLAUDE.md's already-current "Event form field distinctions" / "Booking Flow" sections; keeping both would duplicate, not add
- project_pr28_artist_onboarding — a pre-merge PR code review from 99 days ago; bugs listed were merge-blockers for a since-merged PR, not standing traps
- project_pratiksha_is_pm — people-routing preference; Task 6 sources "key people" directly from CLAUDE.md instead
- user_calls_me_butter — personal preference (nickname), explicitly excluded by the plan

---

## Totals

- Memory files read in full: 72 (73 minus `MEMORY.md`)
- Kept (survived selection test): 57
- Dropped: 15
