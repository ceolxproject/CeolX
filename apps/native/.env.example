# apps/native — environment variable reference
# Only EXPO_PUBLIC_ vars are bundled into the app and visible to users.
# Never put secrets here. All secrets live in apps/server.

EXPO_PUBLIC_API_BASE_URL=http://localhost:3001
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=

# Sentry DSN is hardcoded in apps/native/app/_layout.tsx (not stored here).
# Sentry DSNs are write-only — safe to embed in the app bundle.
# Replace PLACEHOLDER_DSN in _layout.tsx once the ceolx-mobile Sentry project is created.
